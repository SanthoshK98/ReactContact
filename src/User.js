import React from 'react'

const User = (props) => {
    return (
        <div>
            
            <button onClick={props.data}>Call Function</button>
        </div>
    )
}

export default User
